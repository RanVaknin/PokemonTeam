<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/searchBar"
        android:icon="@mipmap/pokedex_foreground"
        android:title="Search"
        app:showAsAction="always|collapseActionView"
        app:actionViewClass="androidx.appcompat.widget.SearchView"/>

    <item android:id="@+id/hamburger"
        android:icon="@mipmap/hamburger_foreground"
        android:title="hamburger"

        app:showAsAction="always|collapseActionView">

    <item android:id="@+id/generateTeam"
        android:title="Generate Team"
        app:showAsAction="never"/>
        <item android:id="@+id/beacon"
            android:title="Enable/Disable Beacon App"
            app:showAsAction="never"/>
    </item>



</menu>